[project]
name = "everybody-codes-py-ft"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "z3-solver>=4.16.0.0",
]

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "black>=26.3.1",
    "isort>=8.0.1",
]
